/**
 * The commands that build the component.
 */
export const buildCommands: ReadonlyArray<string> = [
  'build'
];

/**
 * The commands that build the docs of the component.
 */
export const buildDocsCommands: ReadonlyArray<string> = [
  'build-docs',
  'buildDocs',
  'docs'
];

/**
 * The commands that run the linting.
 */
export const lintCommands: ReadonlyArray<string> = [
  'lint'
];

/**
 * The commands that run the linting.
 */
export const autoAnalyzeCommands: ReadonlyArray<string> = [
  'generate-auto-analysis',
  'generateAutoAnalysis',
  'analyze'
];

/**
 * The commands that run the tests.
 */
export const testCommands: ReadonlyArray<string> = [
  'test'
];

/**
 * The commands that display help to the user.
 */
export const helpCommands: ReadonlyArray<string> = [
  'help',
  '--help',
  '-h',
  '-?'
];
